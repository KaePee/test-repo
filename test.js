//Sample code file
//Js function that searches through an array for the index of an element

function search(arr, val) {
  for (let i = 0; i < arr.length; i++ ) {
    //complete function here
  }
}
"done message"
